const star = parseInt(Math.random() * 5.0) + 3; //3~7사이 랜덤값
console.log('star', star);


// let str = ''   // str를 반복문 안에서 초기화 하면 매번 같은 값을 적용해줌
                  // 삼각형 형식을 랜덤값만큼 반복하기위해 사용
//     for (let i=0; i<star; i++) {
//       str += '*';
//       console.log(str);
//     } 

//삼각형수가 랜덤숫자만큼 반복되게 하기
for (let t = 0; t < star; t++) {   // 🔁 삼각형 전체 반복
  let str = '';
  for (let i = 0; i < star; i++) { // 🔺 삼각형 그리기
    str += '*';
    console.log(str);
  }
}
